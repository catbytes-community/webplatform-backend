{
    "dummy_key": "this is a default incomplete file for config loading using const config = require('config'); in test environment (when running unit tests). this is intended to be incomplete.",
    "aws": {
        "region": "test"
    }
}