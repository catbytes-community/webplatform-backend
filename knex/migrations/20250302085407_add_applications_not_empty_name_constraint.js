/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = async function(knex) {
  await knex.raw(`
    ALTER TABLE applications ADD CONSTRAINT chk_not_empty_name CHECK (name <> '');
  `);
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = async function(knex) {
  await knex.raw(`
    ALTER TABLE applications DROP CONSTRAINT IF EXISTS chk_not_empty_name;
  `);
};